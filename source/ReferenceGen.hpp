#pragma once

#include "Parser.hpp"

// > American Physacolagical Association
// cite: {loop "&" {lastName}}, year
// reference: {lastName, initialFirstName.} (year). title, publisher

// > IEEE
// cite: \[index\], year
// reference: {initialFirstName. lastName and} "title", publisher, `vol.` volume
